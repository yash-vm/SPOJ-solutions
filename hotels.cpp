#include <iostream>
using namespace std;
int main()
{
	std::ios::sync_with_stdio(false);
	int n;
	long long int m;
	cin >> n >> m;
	for(i=0;i<n;++i)
	{
		for(j=i;j<n;++j)
		{
			sum+=s[j];
			if(s>m)
			{
				if(max)
			}
		}
	}	
}