#include <bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	cin >> t;
	while(t--)
	{
		int i,n,c;
		cin >> n >> c;
		int x[n+1];
		x[0]=-1;
		for(i=1;i<n+1;++i)
			cin >> x[i];
		sort(x);
	}
}